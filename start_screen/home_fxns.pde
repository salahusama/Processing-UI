/*
{ 
  if (!drawn) {
    stroke(255, 30);
    
    float row = 0;
    while (row < height)
    {
      line(0, cellHeight * row, width, cellHeight * row);
      row++;
    }
    
    float col = 0;
    while (col < width)
    {
      line(cellWidth * col, 0, cellWidth * col, height);
      col++;
    }
    
    drawn = true;
  }
  
}*/